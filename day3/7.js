//string methods
let name="Pradip"
const log = console.log

log(name)
log(name.length)
log(name.toUpperCase())
log(name.toLowerCase())

//concat
let demo="Shyam"
log(name.concat(" is a friend of "+ demo))

//trim
let demoTrim =" try only"
log(demoTrim)
log(demoTrim.trim())
log(demoTrim.replace("only","done"))
log(demoTrim.trim())
log(demoTrim.slice(3))